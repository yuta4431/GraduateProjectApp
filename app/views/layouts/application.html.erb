<!DOCTYPE html>
<html>
<head>
  <title>RADImemO</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

  <style>
    html, body {
      height: 100%;
      margin: 0;
      background-color: #e1f3f9;
    }

    body {
      display: flex;
      flex-direction: column;
      font-family: Arial, sans-serif;
      min-height: 100vh; /* ページ全体の高さを保つ */
    }

    header {
      background-color: #00a6e9;
      color: white;
      padding: 30px 0;
    }

    nav ul {
      list-style-type: none;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
    }

    nav li {
      margin: 0 30px;
    }

    nav a {
      color: white;
      text-decoration: none;
    }

    nav a:hover {
      text-decoration: underline;
    }

    main {
      flex: 1; /* メインコンテンツが残りの空きスペースを埋める */
      max-width: 1200px;
      margin: 20px auto;
      padding: 20px;
      display: flex;
      flex-direction: column; /* 縦並びにするために追加 */
      align-items: center; /* 中央に配置 */
      gap: 20px; /* 要素間のスペースを設定 */
    }

    footer {
      background-color: #00a6e9;
      color: white;
      padding: 10px 0;
      text-align: center;
      width: 100%;
    }
  </style>
</head>

<body>
  <% if logged_in? %>
    <header>
      <nav>
        <ul>
          <li><%= link_to '新規投稿', new_post_path %></li>
          <li><%= link_to '投稿一覧', posts_path %></li>
          <li><%= link_to '番組登録', new_program_path %></li>
          <li><%= link_to '番組一覧', programs_path %></li>
          <li><%= link_to 'ログアウト', logout_path, data: { turbo_method: :delete, turbo_confirm: 'ログアウトしますか？' } %></li>
        </ul>
      </nav>
    </header>
  <% end %>

  <main>
    <%= yield %>
  </main>

  <footer>
    <p>&copy; 2024 RADImemO</p>
  </footer>
</body>
</html>
